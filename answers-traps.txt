1.13被放在a2寄存器中，而f(8)=12被放置在a1寄存器中，a0指向地址0x7c8,这个地址应该是指向printf函数中第一个输出格式字符串的地址。
2.编译器直接将对f函数的调用结果写在了汇编语言中，可以极大程度上减少函数调用过程中的开销。
3.printf函数的地址是最后一行汇编执行完成之后的pc的值，为0x628
4.ra寄存器的值为0x38
5.HE110 World e110为57616的16进制表示，rld是无符号整数i中每个字节对应的字符(从左往右)，基于最大端应该把i的值修改为0x00726c46,而57616无需修改，因为大小端存放不灰改变它转化为16进制之后的结果
6.我打印出来了8229。该行为是没有定义的行为，因为printf中传入的参数数量少于格式化字符串中的要求的数量。